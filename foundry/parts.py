"""検証済みの部品。**種類 → LCSC の部品番号と、選んだ理由。**

HHKB 分割機（2026-08〜09）で、効く条件を全部並べてデータシートと JLCPCB の
API で確かめたもの。**ここに載っている＝どの機種でも使える、ではない。**
理由のコメントに書いた条件（乾電池 2 本・打ち止め 1.8V など）が自分の機種でも
効くかを読んでから使う。在庫と単価は 2026-08 時点——発注前に
`python -m foundry.jlcpcb_lookup --parts` で取り直す。

機種の spec.py は `PARTS = {**foundry.parts.PARTS, ...}` のように足して使う。
KiCad の Python からも読むので pcbnew を import しないこと。

番号を埋めるときは、JLCPCB の在庫ページで現物を確認してから書く。
型番だけ合っていても、パッケージが違えば実装できない。フットプリントは
pcb/ に出ている実物と突き合わせること。

「基本部品(Basic)」なら段取り費がかからない。「拡張部品(Extended)」は
1 種類あたり段取り費がかかるので、種類を増やすほど高くなる。
"""

# 基板に載らない部品（リード線で繋ぐ電池・電源スイッチ、利用者が挿す XIAO）は
# **BOM にも CPL にも出さない。**JLC に XIAO を実装させない（HHKB fab-checklist）。
NOT_ASSEMBLED = {"xiao_nrf52840", "battery_holder", "wire_pads"}

# 種類 → LCSC の部品番号。**フットプリントは pcb/ の実物と突き合わせた。**
#
# 区分・在庫・単価は JLCPCB の検索 API から取った（2026-08-08）。
# ブラウザが要ると思い込んで 3 回見積もりを外したので、**取り方を残す**:
#
#   POST https://jlcpcb.com/api/overseas-pcb-order/v1/shoppingCart/
#        smtGood/selectSmtComponentList
#   Content-Type: application/json / Origin: https://jlcpcb.com
#   {"currentPage":1,"pageSize":10,"keyword":"C81598","searchSource":"search"}
#
#   componentLibraryType  base=Basic / expand=Extended
#   preferredComponentFlag  true なら Preferred（Extended でも段取り費 $0）
#   stockCount / componentPrices  在庫と数量別価格
#
# ## 費用（5 セット分・2026-08-08 の単価）
#
#   部品代の合計   約 $22.8（¥3,400）
#   段取り費       Extended 3 種 x $3 = $9（¥1,350）… **部品代の 4 割**
#   半田付け       1 点 $0.0017。左 150 点で $0.26。誤差
#
# **効くのは段取り費。**単価ではない。
PARTS = {
    # --- Basic（段取り費 $0）--------------------------------------------
    # 効く parameter は 2 つある。**片方だけ見ると取り替えっこになる。**
    #
    #   (a) 走査電流(約100µA)での VF … 行に届くのは（レール − VF）。
    #       nRF52840 の VIH は 0.7×レール。**レール ≧ VF/0.3 が要る**
    #   (b) 低逆電圧(約1.5V)での IR … ダイオード本来の役目（ゴースト防止）。
    #       プルダウン 13kΩ に対し、同じ行の同時押し n 個ぶんが足し算になる
    #
    # 在庫 5,000 超の SOD-123 ショットキー 29 件と、BAT47/48/49/54/85・
    # 1SS154/181/355/389/400・NSR・RB751・PMEG・MBR05xx・SS12・1N5711・
    # BAS40 を当たった。**(a) と (b) を両方保証しているのは BAT46W だけ。**
    #
    #   型番      (a) VF の保証         (b) IR の保証      122個+段取り
    #   BAT46W    **0.25V @ 0.1mA**    **0.3µA @ 1.5V**   $6.76 ← これ
    #   1N4148W   なし(1mA で 0.715V)  なし(20V で 25nA)  $1.43 Basic
    #   SD103AW   0.37V @ 20mA         なし(30V で 5µA)   $6.75
    #   BAT54W    240mV @ 0.1mA        なし(25V で 2µA)   $6.25
    #
    # **罠: `BAT48ZFILM-FS`（C7436546）は名前だけで中身は SD103AW と同じ。**
    # 型番で判断しないこと。
    #
    # BAT46W にした効果:
    #   打ち止めがマイコン律速に戻る（1.20 → **1.10V/本**）
    #   **壊れ方が「一部のキーが反応しない」から「電源が落ちる」に変わる**
    #   実測が不要になる（保証値で詰まる）
    #
    # 代償（数字で押さえた）:
    #   +約 ¥800（Basic → Extended の $3 込み）
    #   逆漏れ 12 倍。**60℃ で 5 個以上の同時押しだと余裕を割る**
    #     （25℃ なら 9 個でも OK。机上の 25〜30℃ では問題ない）
    #   接合容量 5 倍。行 9 個で 180pF、列切替後 2.31µs で VIL まで落ちる。
    #     SPI 転送 16bit@4MHz = 4.0µs 以上空くので間に合う（余裕 1.7 倍）
    #
    # ## 差し戻し先（バックアップ）
    #
    # **1N4148W（C81598・Basic・$0.0117）。**BAT46W が入手できない、
    # または高温での同時押しが問題になった場合はこちらへ戻す。
    # そのとき打ち止めの計算に使うマトリクスの VF を **0.55** にすること
    # （1N4148 の 100µA 典型 0.50V ＋ 余裕 0.05V）。打ち止めは 1.20V/本 に
    # 上がり、**マトリクスがマイコンより先に落ちる壊れ方に戻る。**
    # フットプリントは同じ SOD-123 なので基板は変わらない。
    "diode":     {"lcsc": "C54110",
                  "desc": "BAT46W SOD-123 ショットキー 100V（VF≦0.25V@0.1mA）"},
    # **効く parameter は Vf。**打ち止め電圧＝使える容量を決める
    # （BATT_V_MIN = 1.7 + 0.1 + Vf）。SOD-123 の候補を全部その軸で比べた
    # （2026-08-08）。**最初は「Basic かどうか」だけで選んでいた。**
    #
    #   型番      区分         耐圧  Vf(1A)  単価     在庫
    #   B5819W    Basic        40V   0.60V   $0.0296  648,363  ← これ
    #   B5818W    Preferred    30V   0.55V   $0.0138   54,542
    #   B5817W    Extended $3  20V   0.45V   $0.0471   32,925
    #   PMEG2005EH Extended $3 20V   もっと低い       フットプリントが別物
    #
    # Vf が最も低いのは B5817W だが **$3 の段取り費**がかかる。10 個のために
    # 割に合わない。B5818W との差は 1A で 0.05V、10mA 換算で 0.02V 程度、
    # 打ち止めに直すと **0.01V/本**で誤差。
    #
    # B5819W を採る理由: SOD-123 で**唯一の Basic**、在庫が 12 倍、そして
    # **手持ちの 1N5819 と定格が一致する（1A 40V）**ので、Task C4 で測った
    # 値をそのまま設計値に使える。
    "schottky":  {"lcsc": "C8598",
                  "desc": "B5819W SOD-123 ショットキー 40V 1A"},
    # パスコンは**負荷（IC）の直近にだけ**置く。効くのは距離ではなく往復ループの
    # 配線長（HHKB #40・#45: 負荷の直近にいない 1 個は誤差にしかならず削除した）。
    "cap_100n":  {"lcsc": "C49678",
                  "desc": "0.1uF 0805 50V X7R (CC0805KRX7R9BB104)"},
    "res_1M":    {"lcsc": "C17514",
                  "desc": "1MOhm 0805 1% 1/8W (0805W8F1004T5E)"},
    # ⚠️ **`cap_100u`（バルク 100µF・C15008）は 2026-08-16 に削除した**
    # （open-gaps #44）。**実測で「守る相手が存在しない」と分かったため。**
    #
    #   BLE の最大瞬間風速  10.22mA（実測・広告＋スキャンの重なり込み）
    #   降下 = 10.22mA × 9.2Ω = 94mV  ← 容量ゼロでもこの値が上限
    #   余裕 300mV に対して 31%（安全率 3.2 倍）
    #   µs 級の尖り  1ns 分解能・60 万点を全数走査して **0 個**
    #
    # **パスコン（cap_100n）は残る。**役割が別（下）。
    # 判断の全体は docs/knowledge/power.md。

    # --- Extended（1 種類 $3）--------------------------------------------
    # **在庫を必ず見ること。**最初 C5184526 を書いたが在庫 0 だった。
    # 在庫があるのは -2（C49352235・37,697 個）。
    "keyswitch": {"lcsc": "C49352235",
                  "desc": "Kailh CPG151101S11-2 MX ホットスワップソケット"},
    # **在庫が最も薄い（2,868 個）。**必要 15 個なので足りるが、
    # 発注時にもう一度見ること（HHKB では 1 日約 48 個ずつ減った）。FH12-12S-0.5SH(54) は製造中止（後継 FH52K）。
    # (55) はまだある。単価 $0.2635 で 15 個 ¥600 程度。安価品（JUSHUO
    # AFC01-S12FCA-00 $0.064）はランドパターンが違い、差し替えられない
    # （縦の総寸法 4.13mm 対 FH12 5.0mm）。**¥600 なので探す必要も無い。**
    "ffc_12p":   {"lcsc": "C88360",
                  "desc": "FFC 12P 0.5mm Hirose FH12-12S-0.5SH(55)"},
    # **74HC ではなく 74LVC。**効く parameter は**動作電圧の下限**。
    #
    # 74HC595 は 2.0〜6.0V。レールは打ち止めで 1.8V まで下がるので、
    # **シフトレジスタが規格外になる**（列が駆動されずキーが反応しない）。
    # 打ち止めを 2.0V に合わせ直すと 1.25V/本となり、アルカリの使える
    # 範囲が大きく減る。
    #
    # SN74LVC595A は **1.1〜3.6V** でレールの全域を覆う。上限 3.6V に対して
    # レールの最大は 2.90V（USB 時 3.3V）で内側。**フットプリントは同じ
    # TSSOP-16 なので基板は変わらない。**+$0.094/個（15 個で +$1.41）。
    #
    # 1.8V での出力電流は ±4mA。列を駆動するには十分（行はプルダウン）。
    # 機種の検査に「全 IC が打ち止めの電圧で動く」を必ず入れる（HHKB の
    # test_every_ic_runs_down_to_the_cutoff がこの見落としを見張っていた）。
    #
    # SOIC-16 の 74HC595（C5947）は Basic だが、上の理由で使えない。
    "74LVC595": {"lcsc": "C52287685",
                 "desc": "SN74LVC595APWR TSSOP-16 (1.1〜3.6V)"},
    # 利用者が挿す MCU。**JLC には実装させない**（NOT_ASSEMBLED）。
    # 技適 211-220207。GPIO は D0〜D10 の 11 本で、ADC は D0〜D5 だけ。
    # BAT 端子は LiPo 充電回路に直結——**乾電池を繋がない**（docs/knowledge/power.md）
    "xiao_nrf52840": {"lcsc": "",
                      "desc": "Seeed XIAO nRF52840（利用者が挿す・BOM に出さない）"},
}
