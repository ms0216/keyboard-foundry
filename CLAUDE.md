# このリポジトリで作業するときに

**自作キーボードを、現物になる前に機械で確かめながら作るための作業場。**
共通の核（`foundry/`）と、機種ごとの `projects/<機種>/` からなる。基板は JLCPCB、
ケースは FDM（K1 Max / A1 mini）、ファームは ZMK（XIAO nRF52840）を前提にしている。

HHKB 分割機（`../2608042258_HHKB_devided`・2026-08〜09）で積み上げたものを抜き出した。
**核は HHKB の発注済みの基板を配置・結線まで再現することを検査で示している**
（tests/test_regression_hhkb.py）。

## まずここを読む

| | |
|---|---|
| [README.md](README.md) | 何があるか・新しい機種の始め方 |
| [docs/method/lessons.md](docs/method/lessons.md) | **失敗から出た規則（具体例つき）。**下の作法の根拠 |
| [docs/knowledge/](docs/knowledge/) | 部品・電源・基板・筐体・ZMK・アンテナ・測り方。**部品を選ぶ前・寸法を決める前に読む** |
| [docs/templates/](docs/templates/) | 台帳の雛形（open-gaps・決定・発注前・部品・暫定値・手順書・引き継ぎ） |
| `projects/<機種>/docs/` | その機種の台帳。**open-gaps.md の冒頭「いま残っているもの」から読む** |

## 基本方針

**現物になってからミスが発覚するのが最悪。**発注も印刷も部品も、利用者のお金と
リードタイムがかかる。だから結論を書く前に機械で確かめ、確かめ方を仕組み（検査・CI・hook）
に落とす。手で叩くだけの検査は存在しないのと同じ。

品質とは、要求（実機の再現・使い勝手）に加えて**ソフトの正しさ・回路の綺麗さ・
ノイズ耐性・組み立てと保守のしやすさ**を含む。

## 検証の作法（HHKB で高くついた教訓。具体例は lessons.md）

1. **結論を書く前に測る。**「〜のはず」と書きそうになったら、その場で測る
2. **通ったことは調べた証拠にならない。**故意に壊して検出できることを確かめてから検査を足す
3. **自分の生成物どうしの一致は検証ではない。**外部の事実（データシート・API・実測・発注済みの実物）と突き合わせる
4. **検査対象に入っていない部品は、検査していないのと同じ。**「全部」と言う前に母数を数える（名前の数ではなく物の数）
5. **設定しただけでは効いていない。**実際に塗られたか・繋がったかまで見る
6. **接頭辞で走査しない。**`D`/`SW` で拾うと電源部を巻き込む（4 回起きた）。`re.fullmatch`
7. **見えるものは見る。**判断の前に自分でレンダリングして Read で見る（基板は hook が出す）
8. **置き換えたら、置き換えられた方を消す。**消した名前でコード・生成物・記録の 3 つを grep して 0 件に
9. **留め方を作ったら「入れられるか・外せるか」も同じ検査で見る。**面の取り合いは体積では見えない
10. **出力を捨てたまま報告しない。戻り値も信用しない。**出来た物を見て判定する
11. **警告を隠さない。**除外するなら除外した数を併記する
12. **決定は前提が動くと期限切れになる。**構造を変えたら、その構造に乗っていた決定を grep して読み直す
13. **部品の事実はデータシートと製品画像で。**自分たちのコメントから推論して断定しない

## 決める前に通す手順（部品を選ぶ・定数を変える・寸法を変えるとき）

「決めた」と言う前に次の 3 つを書く。**1 つでも「まだ」があるなら、まだ決まっていない。**

1. **効く条件を全部列挙してから候補を並べる**（1 軸——安い・在庫・Basic——で選ばない）
2. **影響範囲を信号や力の流れに沿って端から端までたどる**（「隣」で止めない）
3. **各条件を何で確かめたか**（外の事実か、自分の生成物か。後者だけなら確かめていない）

部品は**買う前に本番の品番を決める**。実験用に別品番を買わせない。**型番で買う**。
在庫・区分・単価は推測で書かず `tools/kb <機種> parts`（JLCPCB の API）で取る。
雛形は [docs/templates/decision.md](docs/templates/decision.md)。

## 利用者との進め方

- **理解の確認と実行の許可は別。**利用者が「まず理解を見せて」と言ったら、整理して
  「合っていますか」で止める。許可が出たら、いちいち止めずに完遂まで進めてよい
- **合意の外の条件を黙って足さない。**必要なら実装せずに測定つきで提示する
- **利用者の指示は、自分の道具の答えより優先。**自分の探索が 0 件でも指示を捨てない。疑うのは道具
- 残作業は列挙して振り分けを頼まない。**スコープ内ならやる**、外なら記録があることを確かめて任せる。
  「やりますか」と聞くのは利用者にしか決められないこと（購入・意匠・費用）だけ
- 印刷用のファイルを渡すときは「**回した検査／回していない検査**」を書いて、刷るかの判断を返す
- 「〜の壁」と言われたら、どの部品のどの面かを断面図で特定してから答える
- 利用者は文章より **.blend・断面図・基板の絵**で設計を理解する。作って見せる
- 長い検査の間は待たない。読み取り専用の監査・外部の裏取り・次の編集の準備を並行する
  （ただし**検査中に検査が読むファイルを編集しない**——偽の赤・偽の緑が出る）

## コミットするとき（作業ツリーは複数のセッションが共有しうる）

- **`git add -A` / `git commit -a` を使わない。**触ったファイルだけを名指しで stage する
- コミットの前に **`git diff --cached` を読み**、自分が書いていない行が混ざっていないか見る。
  混ざっていたら、その行が参照しているものが全部入っているかを確かめる（入っていないなら外す）
- commit と push の直前に **`git status -sb` の 1 行目を別のツール呼び出しで読む**
  （別セッションがブランチを切り替える。push は成功して見えて別のブランチに乗る）
- 赤を見つけたら、まず「自分より前から赤か」を見る（`gh run list`）。**そのあと必ず原因まで降りる**
  ——常駐した赤は新しい赤を隠す

## 新しい機種を始める

```
.venv/bin/python3 tools/new_project.py <名前> <KLE の JSON>   # 島が 2 つなら left/right
```

1. `projects/<名前>/spec.py` を決める（値ごとに理由をコメントに。推定には `[暫定]`）
2. 要求を書く: 何を再現し何を妥協するか（HHKB の「揺らがせない寸法」に当たるもの）を
   spec.py の冒頭か docs/ に。**要求は検査にする**（HHKB の test_invariants）
3. `tools/kb <名前> plate` → `build/<名前>/plate_*.png` を**見る**
4. `tools/kb <名前> pcb` → hook が絵を出す → 見る。MCU・電源は `pcb_extra.py`
5. 電源・行列の方式は [docs/knowledge/power.md](docs/knowledge/power.md) を読んでから決める。
   試作は [docs/knowledge/measurement.md](docs/knowledge/measurement.md) の順で
6. `spec.ZMK["pins"]` を書いて `python -m foundry.zmk <名前>`。push すると CI が .uf2 を作る
7. ケースは HHKB の gen_case / gen_assembly を参照実装に機種の中で書く
   （[docs/knowledge/case-and-print.md](docs/knowledge/case-and-print.md)）。
   **2 台目で共通になった部分は foundry/ へ抜き出す**（抜き出したら HHKB の再現検査を通す）
8. 発注の前: `tools/kb <名前> gate` と `docs/fab-checklist.md` を上から

## よく使うコマンド

```
.venv/bin/pytest tests -q                  # 全検査（KiCad があれば基板の生成と DRC も・約 3 分半）
.venv/bin/pytest tests -q -m "not slow"   # 編集中の速い回（5 秒を超えるケースの形状の検査を飛ばす）
REQUIRE_KICAD=1 .venv/bin/pytest tests -q  # KiCad の要る検査が飛んだら赤にする
tools/kb <機種> plate|pcb|zmk|fab-fields|drc|render|slice|gate|parts
.venv/bin/python3 -m foundry.check_zmk_config   # ZMK の設定を push 前に検査
.venv/bin/python3 -m foundry.mutate foundry/mech.py projects/<機種>/spec.py   # 定数を壊して検査が気づくか（遅い）
.venv/bin/python3 -m foundry.jlcpcb_lookup "100nF 0805"   # 部品を探す（区分・在庫・単価）
```

`tools/kb` が venv と KiCad の Python（pcbnew）を使い分ける。道具の場所は
[foundry/paths.py](foundry/paths.py)（環境変数で上書きできる）。

## 核（foundry/）を直すとき

- **HHKB の再現検査（tests/test_regression_hhkb.py）を必ず通す。**これが核の回帰の物差し
- KiCad の Python から読むモジュールは**標準ライブラリだけ・3.9 の文法**（test_meta が見る）
- 同じ値を 2 か所に書かない。定数は使うか消すか `[記録のみ]` と書く（test_meta が見る）
- 生成器は寸法を持たない。寸法は spec.py と mech.py、配列は layout、行列は matrix
