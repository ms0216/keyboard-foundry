# CCKB（Completely Compact KeyBoard）設計書

2026-09-23 利用者と合意。**HHKB 英語配列に準拠し、出張先や会議室へ持ち運ぶための、薄く軽い一体型キーボード。**
HHKB（Professional HYBRID Type-S・294×120×40mm・540g）は机に据えて使い、CCKB は鞄に入れて持ち出す。

完了とは**発注できる状態**（下の §9）。発注・印刷・購入は利用者が判断する。

---

## 1. 要求

### 守るもの（検査にする。設計の都合で崩すと検査が落ちる）

| 項目 | 値 | 照合する外の事実 |
|---|---|---|
| キー配置 | HHKB 英語配列の全キーの中心位置（スペースを除く） | `../2608042258_HHKB_devided/layout/hhkb_original.json`（QMK と Wikimedia の KLE 図で裏付け済み） |
| キーピッチ | 19.05mm | HHKB 公称 |
| 行ずれ | HHKB と同一 | 同上 |
| 最下段の空き | 左 1.5u・右 2.5u（左右非対称。KLE で確認） | 同上 |
| スペースの範囲 | 4.0〜10.0u を 3 キーで隙間なく覆う | 同上 |

### 妥協したもの（受け入れた差）

| 差 | 理由 |
|---|---|
| スペース 6u → **2.25u＋1.5u＋2.25u**。既定は 3 つともスペース、真ん中は後でキーマップで変えられる | 低背の 6u キャップ・スタビがどの系列にも無い（調査 2026-09-23）。この 3 サイズは CFX で揃う |
| 段ごとのキー高さ・打鍵面の傾き 7.3° を再現しない | 低背スイッチでは作れない。ほぼ平ら |
| 静電容量無接点 → Kailh Choc V1 | 薄さ |
| 単3×2 → CR2450 | 薄さ・軽さ。LiPo は利用者が不採用（発火・劣化） |

### 目標（検査で数値を出して報告する。上限を決めるのは利用者）

- 外形 約 292×105mm・厚さ 約 12mm（キーキャップ上面まで）
- 重さ 約 300〜430g（スイッチ・キャップの単重が未公表。部品が届いたら量る）

---

## 2. 決定と根拠

| # | 決めたこと | 根拠（外の事実） | 退けた案 |
|---|---|---|---|
| D1 | **一体型** | 分割の親側を CR2450 で動かすと、容量の約 4 割（約 250mAh）で内部抵抗が 25Ω を超え（Energizer CR2450 データシートの IR 曲線）、起動に 2.6〜2.7V が要る領域（HHKB task-c4-c5 ② 実測）に入る。復帰が確率的になる。片側不在で 3.7mA（HHKB task-c3 §5-4 実測） | 分割＋合体（+20〜25g・親側の電池 約 6 ヶ月）。**この結論は「親側が走査し続ける ZMK の仕様」と「起動電圧の実測」に乗っている。どちらかが変われば読み直す** |
| D2 | **Kailh Choc V1（PG1350）・19.05mm ピッチ** | 19.05 ピッチ用のキャップ（Chosfox CFX）が 1/1.5/1.75/2.25u で国内 1 店に揃う。Choc スタビも同店 | Choc V2・Gateron KS-33（MX キャップの干渉・国内在庫・ソケット） |
| D3 | **スイッチを直接はんだ付け** | どのみち手はんだ（Choc ソケットは JLCPCB・LCSC とも在庫 0、2026-09-23 API。持ち込み実装は $85＋通関＋送料）。直接なら 約 1.2mm 薄く・約 9g 軽く、ソケットの −10℃・2V・100 回の制約が消え、プレート・スイッチ・基板が一体になる | ホットスワップ（差額は部品代 約 ¥1,300） |
| D4 | **CR2450・一体型** | 5.0mm 厚・6.8g・620mAh（データシート）。ホルダ MY-2450-01（C964845）在庫 5,908 | 単4×2（+16g・厚い）、CR2032（寿命 約 1/3） |
| D5 | **XIAO nRF52840 ＋ 74LVC595 ×2** | HHKB で実機確認済みの構成。技適 211-220207 | — |
| D6 | **XIAO と電池は最下段の空いた角に置き、上面は平らに。電池は裏ぶたから替える** | 角にはキーが無いので高さを使える。利用者の希望（表面を簡素に） | 上面のふた |
| D7 | 名前 **CCKB**（シールド名 `cckb`） | 利用者の命名。HHKB へのリスペクト。他社の商標は刷らない | tabi |

---

## 3. 部品

| 部品 | 品番 | 数 | 入手 |
|---|---|---|---|
| スイッチ | Kailh Choc V1 PG1350（軸色は利用者が選ぶ） | 62 | 遊舎工房 |
| キーキャップ | Chosfox CFX 1u / 1.5u / 1.75u / 2.25u | 下表 | 遊舎工房 |
| スタビライザー | Kailh Choc 2u プレートマウント | 4（Enter・左 Shift・スペース左右） | 遊舎工房 |
| マイコン | Seeed XIAO nRF52840 | 1 | 手はんだ |
| 電池 | CR2450 | 1 | 量販店・ダイソー大型店 |
| JLCPCB 実装 | BAT46W（C54110）×62・SN74LVC595APWR（C52287685）×2・B5819W・MY-2450-01（C964845）・スライドスイッチ・分圧 1MΩ×2・パスコン | | 発注直前に `tools/kb cckb parts` で在庫を取り直す |
| プレート | FR4 1.2mm（JLCPCB に基板として発注） | 1 | Choc スタビが 1.2mm を要求 |
| ケース | PLA（K1 Max。長さ 約 292mm で A1 mini には入らない） | 上枠・底板 | 利用者が印刷 |

キャップの内訳（62 キー）: 1u ×51・1.5u ×4（Tab・Del・Meta×2）＋スペース中央 1・1.75u ×2（Ctrl・右 Shift）・2.25u ×2（Enter・左 Shift）＋スペース左右 2。

---

## 4. 回路

```
CR2450 → スライドスイッチ → [分圧 1MΩ+1MΩ → XIAO の ADC] → B5819W → XIAO 3V3
                                                               （BAT 端子には繋がない）
行 5 本 ── XIAO 直結（入力・プルダウン）
列 15 本 ── 74LVC595 ×2（SPI: SCK=D8・MOSI=D10・CS=D7。D9 は MISO に確保）
キーごとに BAT46W（col2row）
```

- GPIO は 11 本中 9 本（行 5・SPI 3・ADC 1）
- 行列は 5×15。キーの割り当ては `foundry/matrix.py`（physical）を初期値にする
- 電源スイッチは必須（鞄の中でキーが押されて起きるのを止める）
- ショットキーは USB 給電中に一次電池を充電しないため（HHKB の経路）

## 5. 機構

### 積み重ね（下から）

1. 底板（PLA）。CR2450 の裏ぶたを含む
2. 基板（**1.2 か 1.6mm。§8 の検査で決める**）。スイッチの足が約 0.6〜1.0mm 下に出る
3. プレート（FR4 1.2mm）。基板上面からプレート上面まで 2.2mm（Choc 規格）
4. スイッチ・キーキャップ

**プレート・スイッチ・基板ははんだで一体になる。**これを上枠と底板で挟む。

### 角（この設計の中心）

| | 左の角 | 右の角 |
|---|---|---|
| 大きさ（最下段の空き） | 1.5u×1u ＝ 28.6×19.05mm | 2.5u×1u ＝ 47.6×19.05mm |
| 置くもの | XIAO（21×17.8mm）。USB-C は左側面へ | CR2450（φ24.5）・電源スイッチ（側面から操作） |
| 上面 | 上枠と同じ平らな面 | 同左。電池は裏ぶたから |

- 上枠は角を覆うので、プレートと基板の一体物は**下から入れる**（上枠 → 一体物 → 底板の順にネジで留める）
- XIAO のアンテナは USB と反対の端（内側を向く）。アンテナ直下の銅を抜く（docs/knowledge/rf-antenna.md）

## 6. ファーム（ZMK）

- シールド `cckb`・ボード `xiao_ble//zmk`・CI で .uf2 を作る
- キーマップ: HHKB の既定（ベース面・Fn 面）。スペース 3 つとも `&kp SPACE`。`&bootloader`・`&bt BT_CLR` を置く
- 電池残量: `firmware/` の残量計に CR2450（Li/MnO2）の放電曲線を足す（アルカリの曲線のままだと値が嘘になる）
- 打ち止め: 電池 **2.0V ＋ Vf** で自分から電源を落とす（`low_battery_off`。値は試作の起動試験で見直す）
- 設定は HHKB の定石（docs/knowledge/zmk-and-xiao.md）: スリープ 30 分・Apple §58.6 適合の接続間隔など

## 7. 核（foundry/）の改修

いまの核は MX 専用（mech.py の開口 14.0・SWITCH_FP・Cherry スタビ・pcb.py の Value "CPG151101S11-2"・DIODE_OFFSET）。

- spec.py に **`SWITCH = "mx_hotswap" | "choc_v1"`** を足し、スイッチの種類ごとの規格値（開口・フットプリント・スタビの形と間隔・ダイオード位置・部品番号）を mech.py の表にまとめる
- 既定値で埋めない（`REQUIRED` に入れる）。HHKB の fixture spec には `"mx_hotswap"` を書く
- Choc の直付けフットプリントは kiswitch（perigoso）の上流から取り、**端子の位置をデータシート（φ1.2 を (5.90, 0)・(3.80, 5.00)、中心穴 φ3.4、ボス φ1.9 を ±5.5）と突き合わせる検査**を置く
- **HHKB の再現検査（tests/test_regression_hhkb.py）を通す**
- 生成器は寸法を持たない（寸法は spec.py と mech.py）

## 8. 決まっていないこと（作図・計算で決める。決まったら §2 に移す）

| # | 何 | 決め方 | 変わったら利用者に相談する条件 |
|---|---|---|---|
| O1 | 右の角に CR2450（φ24.5）が 19.05mm の段に収まるか。手前の縁を何 mm にするか | 平面図と断面図を描く。案: 手前の縁を広げる／電池を奥にずらし 1.75u Shift の下へ潜らせない範囲で置く | 奥行が 110mm を超える、または電池を CR2032 に替える必要が出たとき |
| O2 | CFX の 2.25u キャップに Choc スタビのステムが合うか | 製品画像と寸法で確かめる。確かめられなければ部品を先に少数買って見る（本番の品番） | 合わないとき（スペースの分け方が変わる） |
| O3 | 基板の厚さ 1.2 / 1.6mm | 打鍵荷重でのたわみを、ネジ位置を入れて見積もる | — |
| O4 | ネジの位置と数 | 5 条件（縁・開口・部品・コートヤード・配線）を同時に満たす | — |
| O5 | XIAO の USB-C に市販のケーブルが届くか | 組み立てモデルにケーブルを入れて検査する | — |
| O6 | 一体型の起動電流（未測定） | **試作で測る**（§10）。机上では HHKB の右側（ペリフェラル）の実測 5.5mA×約 1.3 秒を上限の目安にする | 起動試験に落ちたとき |

## 9. 完了条件（発注できる状態）

1. 基板の発注データ: 配線済み・**DRC の違反と警告を全部数えて記録**・LCSC 番号・実装面・CPL（Fabrication Toolkit に通して出力を読む）
2. プレートの発注データ（FR4 1.2mm）
3. ケース（上枠・底板・裏ぶた）の STL を **OrcaSlicer で実際にスライス**したもの
4. ファームの .uf2（GitHub Actions でビルド成功。実機は未確認と明記）
5. 利用者が買う部品の一覧（型番・店・数・概算）
6. 組み立て手順書（プレート → スタビ → スイッチを全部はめてから はんだ付け、の順を守る）
7. 試作の試験手順（§10）
8. `tools/kb cckb gate` が通る。`.venv/bin/pytest tests -q` が全部緑（KiCad の要る検査も `REQUIRE_KICAD=1` で）

## 10. 試作で測ること（発注後。手順書にする）

- 起動・スリープ復帰: 常温／冷蔵庫で 2h 冷やした電池／電池の残りが少ない状態（直列抵抗 10・25・40Ω で模擬）
- 平均電流（接続中・打鍵中・スリープ）→ 電池寿命の見積もりを実測で置き換える
- 全キーの疎通とゴースト（同時押し）
- RSSI（XIAO 単体と組み込み後の差。docs/knowledge/rf-antenna.md の手順）
- 重さ・寸法を量り §1 の目標と比べる

## 11. 検査の計画（作るもの）

| 検査 | 見るもの | 故意に壊して落ちることを確かめる |
|---|---|---|
| 配列の不変条件 | 全キー中心が HHKB の KLE と一致（スペースは範囲を覆う）・ピッチ・最下段の空き | キーを 1 つずらす |
| Choc の規格 | フットプリントの端子・穴がデータシートと一致・プレート開口 13.8 | 値を 0.1 ずらす |
| 行列とファーム | 基板のネットとシールドの行列表が、**物理位置**と一致 | 2 キー入れ替える |
| 結線 | 宣言 → パッド → ネットを端から端まで。ネットの無いパッド 0 | 1 本外す |
| 組み立て | 全部品（ケーブル・電池を含む）の干渉 0・電池を裏ぶたから入れて出せる・USB が挿さる・ネジにドライバーが届く | 電池を 1mm 大きくする |
| 印刷 | 肉厚 ≧1.2・水密・スライス成功 | 壁を 0.8 にする |
| 発注の門 | open-gaps の「せき止めているもの」が 0 | 行を 1 つ足す |

## 12. 配線の方針

- キーの行列は**道具が決まった形で引く**（行＝裏面の横、列＝表面の縦。スイッチ → ダイオードの L 字）。道具は写すだけにし、配線を加工する判定を書かない
- XIAO・595・電源まわりは Freerouting 2.3.0（手元に jar あり。**Java（OpenJDK）を Homebrew で入れる**）
- 2 層・両面 GND ベタ。塗った後の面積で健全性を見る。アンテナ直下の禁止域は塗られた後のベタで確かめる
- DRC 0 は配線が良いことを意味しない。層ごとの長さ・ビア数・GND の島・遠回りの比を数字で出し、絵で見る
