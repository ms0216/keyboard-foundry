"""CCKB（Completely Compact KeyBoard）の設計値。**寸法・部品・行列の決定はここに集める。**

持ち運び用の HHKB 英語配列準拠キーボード。設計書: docs/superpowers/specs/2026-09-23-cckb-design.md
要求（HHKB 準拠）は tests/test_cckb.py が HHKB の実物の KLE と突き合わせる。
基板・プレート・ケースの境界（角・ネジ・電源スイッチ・スタビの逃げ・継ぎ目・高さ）は
docs/decisions/2026-09-24-interface.md で決め、tests/test_cckb_interface.py が守る。
値ごとに**なぜその値か**を書く。`[暫定]` は docs/provisional-values.md に同じ数値で載せる。

座標はすべて CAD（キー領域の中心が原点・X 右・Y 奥・mm）。キー領域は 15u × 5u
（±142.875 × ±47.625）。高さ Z は机（ケースの底面）が 0。
"""

NAME = "CCKB"                # 基板に刷る名前（他社の商標 HHKB は刷らない）
LAYOUT = "layout.json"       # HHKB 英語配列のスペースを 2.25+1.5+2.25u に分けたもの
PIECES = ("main",)           # 一体型（設計書 D1）
SWITCH = "choc_v2"           # Kailh Choc V2 直付け（静音 PG1353S も同じ足跡で受ける。決定記録 2026-09-25-choc-v2）

# ---------------------------------------------------------------------------
# 外形（平面）。ケースは「トレイ（底＋外壁）」に基板とプレートを上から落とす（決定記録 §5）
# ---------------------------------------------------------------------------
# 角のふたの内側の辺を、角のセルの境目から離す量（interface.cover）。**外面の位置もこれで決まる**:
# 外面 = キー領域 ＋ CASE_KEY_GAP ＋ 1.6 = ±144.975（XIAO_AT はこの外面から決めた。
# 2026-09-24 の監査で壁の内面を動かしたときも外面は動かしていない → CASE_WALL）
CASE_KEY_GAP = 0.5
# 基板（とプレート）の外形と、トレイの壁の内面の隙（片側）。**境界の値はここ 1 か所**で、
# interface.wall_inner（= 基板の外形 ＋ これ）と case.r_in（壁の内面の角の丸み = CORNER_R ＋ これ）が
# interface を通して読む。**前は 0.2** で、JLC の外形公差 ±0.2（pcb-capabilities の CNC 通常精度）と
# PLA の縮み 0.2〜0.3% を足すと奥行きで 0 以下になりえた（監査 E 重要 2・裁定 R5）。
# 上から見えるキャップ↔壁の隙間は KEYCAP_GAP 0.525 ＋ 0.3 ＋ 0.5 = 1.325（HHKB 実機の実測 1.0〜1.5 の中）
CASE_PCB_GAP = 0.5
# 外壁の厚さ。外面（上）を動かさずに内面を CASE_PCB_GAP まで外へ出したので 1.6 → 1.3（0.4 ノズル
# 3 周・肉厚の下限 1.2 の内側）。**USB-C の上限**: メスの口をケースの外面から USB_RECESS だけ
# 引っ込めると、XIAO の基板の端は外面から 0.3 + 1.53 = 1.83 の位置に来る。壁がそれより厚いと
# XIAO の基板が壁に食い込む（2.4 だと 0.57 食い込む）
CASE_WALL = 1.3
# プレートと基板の外形はキー領域から 0.3 外（壁の内面からは CASE_PCB_GAP 内側）。
# **上から落とすので縁を押さえる枠は無い**（プレートはスイッチ 62 個のはんだで基板に留まる。D11）
PLATE_MARGIN_X = 0.3
# 開口と外形の間に残す板の最小（0.4 ノズル 3 本）。これより細い帯は残さず、開口を外形の外まで伸ばす
# （V2 のスペースのスタビの羽の手前の端が外形から 0.225 だった。foundry.plate が読む）
PLATE_MIN_WEB = 1.2
PLATE_MARGIN_Y = 0.3
# プレート・基板の角の丸み。トレイの内側の角（壁の内面）は同心の R（CORNER_R ＋ CASE_PCB_GAP = 1.5）
CORNER_R = 1.0
PCB_INSET_X = 0.0            # 基板＝プレートの外形。角（XIAO・電池）は基板の上にある
PCB_INSET_Y = 0.0

# ---------------------------------------------------------------------------
# 高さ（Z）の積み上げ。机 = 0
# ---------------------------------------------------------------------------
# 底（トレイの床）。0.4×3 = 1.2（肉厚の下限）。**前は 1.6**（滑り止めのくぼみ 0.4 を床全体で
# 引いていた）。2026-09-24 利用者の決定（O10）で 1.2 にし、くぼみの所だけ床を ANTISLIP_RECESS
# 厚くした島（上面 = CASE_FLOOR ＋ ANTISLIP_RECESS = 1.6。interface.z の island_top）にする。
# くぼみの下にも 1.2 残る。島は裏に出る物（スイッチの足・裏の部品・電源スイッチの足）の下を避けて
# 置く（case_spec.ANTISLIP_AT）。全体は 0.4 薄くなる（14.2 → 13.8）。床のたわみは決定記録
# 2026-09-24-interface.md §2-5-1
CASE_FLOOR = 1.2
ANTISLIP_RECESS = 0.4        # 四隅の滑り止めシートを貼るくぼみ（D10）。島の上で床の上面から引く
ANTISLIP_SHEET_T = 0.5       # [暫定] 買うシートの厚さ。くぼみより 0.1 厚く出て接地する
# 床の上面から基板の下面まで。**V1 のまま 1.8**（2026-09-25 利用者の決定: V2 でも空きは増やさず、
# 床の内側に 0.4 の止まり穴を掘る。決定記録 2026-09-25-choc-v2 §4-1 の案 2）。
# 基板の下へ出る物（基板の上面から。最悪は基板 1.44・長さは公差の上限）:
#   V2 の中心の突起 φ4.80 × 3.30（±0.10）→ 基板の下面から 1.96 → **床の穴**（interface.floor_pockets）
#   スタビの箱 3.30（公差は図に無い）→ 1.86 → **床の穴**
#   V2 の足（金属 2・位置決め 1）3.00±0.2 → 1.76 → 床の上面と 0.04（**穴を掘っていない。V1 で決めた余裕 0.1 を割る**。
#     決めるのはケースの段と利用者: 足の下にも穴を掘るか、0.04 を認めるか。open-gaps）
# 裏の部品は BOTTOM_PART_H 以下、電源スイッチの足は切って PSW_PIN_TRIM（下）
UNDER_PCB = 1.8
# 床の内側（上面）に掘る止まり穴の深さ。外の底面は平らのまま（床 1.2 のうち 0.8 残る。0.2 層で 4 層）。
# 中心の突起 1.96 は床の上面から 0.24、スタビの箱 1.86 は 0.34 上に止まる
FLOOR_POCKET_DEPTH = 0.4
# 穴を物の外形からどれだけ広げるか（半径）。突起は基板の穴 φ5.05 の中で 0.125 ずれ、基板は取付のネジ
# （穴 2.2・ネジ 2.0）で 0.1 ずれ、刷った穴は縮む（0.2）。スタビの箱も同じに扱う
FLOOR_POCKET_CLEAR = 0.4      # [暫定] 上の理由。試し刷りの小片で確かめる
# 裏の部品（JLC の実装）の背の上限（組み立てモデルの包絡）。データシートの最大: BAT46W の SOD-123
# 1.25（CJ の寸法 A）・D_SOD-123 の汎用 1.35・0805 の C 1.35・TSSOP-16 1.2。前は電源スイッチ（裏・1.45）が
# 一番背が高く、その値で包んでいた（2026-09-24 に電源スイッチを表へ移した）
BOTTOM_PART_H = 1.35
# 基板の厚さの公差（**割合**。JLCPCB の PCB Capabilities: 厚さ 1.0mm 以上は ±10%
# <https://jlcpcb.com/capabilities/pcb-capabilities>「For the 1.6mm board thickness, the
# finished board thickness ranges from 1.44mm(T-1.6×10%) to 1.76mm(T+1.6×10%)」）。
# 絶対値（1.6 基準）は PCB_T_TOL_ABS = 0.16（ケースの寸法を積むときはこちら）
PCB_T_TOL = 0.1
SWITCH_PIN_TOL = 0.2         # 足の長さの公差（Kailh 図面 CPG135301D01・CPG1353S01D0x の側面図「3−3.00±0.2」）
# 基板の厚さ（O3）。1.2 と 1.6 で**全体の厚さは同じ**（効いているのはスイッチの足の長さで、
# 足は基板の上面から 3.0 出るので、薄い基板ほど下の空きが要る）。たわみは 1.6 が
# 1.2 の (1.6/1.2)^3 = 2.37 倍硬い。計算は決定記録 §2-4。重さは 1.6 が約 21g 重い
PCB_T = 1.6
PCB_T_TOL_ABS = PCB_T * PCB_T_TOL   # 0.16（JLC ±10% を 1.6 で絶対値にした。psw_tip_range が積む）
# Choc V2 の高さ（Kailh 図面 CPG135301D01・静音 CPG1353S01D01-01 / D02-01 の側面図・正面図。3 枚で同じ）。
# 基板の上面から（ハウジングの下面が基板に着く読み。下面の「0.30±0.2」の正体は未確認・決定記録 §8 の 1）
PLATE_TOP_ABOVE_PCB = 2.2    # プレートの上面（正面図の 2.20。プレート 1.2 の下に 1.0 空く）
SWITCH_TOP_ABOVE_PCB = 5.3   # ハウジングの上面（側面図の 5.30）。静音はその上に 0.4 のつば（5.70）がある
SWITCH_STEM_ABOVE_PCB = 8.6  # ステムの上面（正面図 6.40 ＋ 2.20）。全行程は標準 3.2・静音 2.8
SWITCH_PIN_L = 3.0           # 足の長さ（基板の上面から下へ。側面図「3−3.00±0.2」）
SWITCH_STUD_L = 3.3          # 中心の突起の長さ（基板の上面から下へ。側面図 3.30。一般公差 L≦5 の ±0.10）
SWITCH_STUD_TOL = 0.1
SWITCH_STUD_D = 4.8          # 中心の突起の直径（側面図 φ4.80）
# 全行程（静音 CPG1353S01D01-01 / D02-01 の仕様欄 9「Total travel: 2.8±0.25」。標準は 3.2±0.25）。
# 利用者が買うのは静音（決定記録 2026-09-25-choc-v2 §10-1）。押し切ったキャップは最大 3.05 沈む
SWITCH_TRAVEL = 2.8
SWITCH_TRAVEL_TOL = 0.25
# 静音のステムの周りのつば（ハウジングの上 0.40。側面図 5.70 = プレートの上 3.50 ＋ 2.20）。
# 径は図に数字が無く、上面図を縮尺で読んで約 7.9（研究の図 build/cckb/v1v2_sockets.png）
SWITCH_COLLAR_ABOVE_PCB = 5.7
SWITCH_COLLAR_D = 7.9        # [暫定] 上の理由。見本をノギスで
# キーキャップ（自分で刷る・D14。形は後の段で決める）
KEYCAP_GAP = 0.525           # [暫定] セルの境目からキャップの縁まで（18.0 角）
KEYCAP_TOP_T = 1.2           # [暫定] ステムの上面からキャップの上面まで
# 縁（トレイの壁の上面・角のふたの上面）。基板の上面から。**電池で決まる**:
# ホルダ 5.5 ＋ ふたとの隙 0.3 ＋ ふた LID_T 1.2 = 7.0。キャップを底まで押した上面
# （ハウジングの上面 5.3 ＋ 1.2 = 6.5）より 0.5 高く、鞄の中でキーが押されにくい（V1 は 0.8。V2 で 0.3 減った）
RIM_ABOVE_PCB = 7.0
LID_T = 1.2                  # 角のふたの天板（0.4×3）

# ---------------------------------------------------------------------------
# 左の角: XIAO nRF52840（表に平らにはんだ付け・キャステレーション）
# ---------------------------------------------------------------------------
# 寸法は Seeed 公式の STEP（lib/xiao.3dshapes・無改変）を build123d で測った値。
# 実物のノギス（HHKB 2026-08-08）21×18×3・USB 込み 4.5 と一致
XIAO_L = 20.95               # 基板の長さ（USB ←→ アンテナ）
XIAO_W = 17.78               # 基板の幅
XIAO_H = 4.47                # 下面から USB シェルの上面まで（基板 1.25 ＋ シェル）
XIAO_USB_OVERHANG = 1.53     # USB シェルの口が基板の端から出る量
XIAO_USB_W = 8.94            # USB シェルの外形（幅）
XIAO_USB_H = 3.2             # 同（高さ）。下面は XIAO の上面（1.25）付近
XIAO_USB_D = 7.3             # 同（奥行）
XIAO_USB_Z = 2.87            # USB シェルの中心の高さ（XIAO の下面から）
XIAO_ANT_FROM_EDGE = (0.27, 1.87)   # アンテナのチップ（1.6×3.2）の、アンテナ側の端からの範囲
XIAO_ANT_SIDE = (0.21, 3.41)        # 同、幅方向（中心から D0〜D6 の列の側へ）
# USB の口をケースの外面からどれだけ引っ込めるか。HHKB の実測: 挿しきったプラグは
# 樹脂と口の間に金属が 1.0 見える（envelopes.USB_SHELL_EXPOSED）→ 1.0 までなら届く。
# 印刷公差 ±0.2 を見て 0.3（HHKB gen_case.USB_RECESS と同じ）
USB_RECESS = 0.3
# 壁の USB の口の逃げ（片側）。HHKB gen_case.PORT_CLEAR（利用者「大きすぎる」で 0.6→0.4）
PORT_CLEAR = 0.4
# XIAO の中心。x = 外面 −144.975 ＋ USB_RECESS ＋ 張り出し 1.53 ＋ 半長 10.475。
# y は角のセルの中心（前は基板の銅の限界 −47.625、奥はプレートの開口 −28.575。
# 差 19.05 − 17.78 = 1.27 をパッドの張り出し 0.6 ずつに使う）
XIAO_AT = (-132.67, -38.1)
XIAO_PAD_EXT = 0.6           # 表面実装のパッドを XIAO の外形から外へ出す量（こて先用）
# ピンの並び（lib/xiao.pretty: 7 本を 2.54 おき・端から端 15.24、パッド φ1.7）。長手の中心から
XIAO_PIN_SPAN = 15.24
XIAO_PAD_L = 1.7
# パッドが XIAO の縁から内へ入る長さ。XIAO のキャステレーションは半円まで含めて縁から 2.032
# （公式 STEP の面の辺）。Seeed 公式のフットプリント XIAO-nRF52840-SMD のランドは縁から内へ
# 2.18（2.75 長・ピンの並びの中心から ±8.0825）なので、それに揃えて 2.2。**前は 2.85** で、
# 余った 0.82 が裏の露出パッド NFC2 の真下まで届いていた（監査 D 重要 1）。2.2 なら D7 のパッドの
# 奥の端と NFC2 の間は 8.89 − 2.2 − 6.258 = 0.432（XIAO_BOTTOM_CLEAR 0.3 より広い）。
# lib/xiao.pretty/XIAO_nRF52840_SMD（パッド 2.8 = 2.2 ＋ XIAO_PAD_EXT 0.6）と同じ値。
# tests/test_cckb_pcb.py が板の上のパッドで確かめる
XIAO_PAD_IN = 2.2
# アンテナの下の銅を全層で抜く範囲（アンテナ側の端から内へ・外へ、幅は中心から ±）。
# 内 3.14 は Seeed 自身が XIAO の中で抜いている深さ（HHKB 決定 2026-08-16 の抽出値）
ANT_KEEPOUT_IN = 3.14
ANT_KEEPOUT_OUT = 5.0        # [暫定] 外（Alt の側）へ。HHKB の「電池から 5mm」の実測を借りた
ANT_KEEPOUT_HALF_W = 6.0     # [暫定] チップ（中心から 0.21〜3.41）に 2.6 足した
# XIAO の裏の露出パッド 8 個（キャステレーション 14 個とは別）。**板の上の向き（USB が −x・
# D0〜D6 が −y）で、XIAO の本体の中心から (x0, y0, x1, y1)。**公式 STEP（lib/xiao.3dshapes）の
# 下面の平らな面と、Seeed 公式の KiCad フットプリント XIAO-nRF52840-SMD のランド（パッド 15〜22・
# tests/fixtures/seeed_xiao）の**和**。名前は同じライブラリの記号のピン名。
# 表の銅（ベタ・配線・ビア・パッド）をここから XIAO_BOTTOM_CLEAR 以内に置かない。
# **前は 6 個と数えていて**（NFC の 2 個が抜けた）、NFC2 の真下に D7 のパッドと GND のビアが
# あった（監査 D 重要 1・2026-09-24）。tests/test_cckb_pcb.py が STEP と Seeed を毎回読み直して
# この表と突き合わせ、8 個それぞれの下の表の銅が 0 であることを見る
XIAO_BOTTOM_PADS = {
    "15": ("SWDIO", (-9.354, -2.148, -7.655, -0.448)),
    "16": ("SWCLK", (-9.354, 0.392, -7.655, 2.092)),
    "17": ("EN", (-6.815, -2.148, -5.115, -0.448)),
    "18": ("GND", (-6.815, 0.392, -5.115, 2.092)),
    "19": ("VBAT", (-0.863, -5.723, 0.301, -3.223)),
    "20": ("GND", (-2.768, -5.723, -1.605, -3.223)),
    "21": ("NFC1", (8.051, 3.232, 10.551, 4.353)),
    "22": ("NFC2", (8.051, 5.138, 10.551, 6.258)),
}
# 露出パッドから表の銅まで。手で置くずれ（パッドに合わせて ±0.1 程度）とマスクの位置の公差を見る。
# 監査 D の提案「露出パッドから 0.3 以上」
XIAO_BOTTOM_CLEAR = 0.3
# XIAO の裏に出ている USB-C のシールドの足の**めっきの長穴** 4 個（GND・はんだが付いている。
# 監査 D 2 回目の軽微 4）。露出パッド 8 個とは別に数える。XIAO_BOTTOM_PADS と同じ座標で。
# 出典: Seeed の裏面の図面 Bottom-pad-positioning.zip の Dimensioning.dxf（Eagle の brd から出した物）の
# LONGP100 ×4。長円 2.0×1.0 を 0.9144 倍（USB から遠い 2 個）・0.889 倍（USB の端の 2 個）、
# 中心は長手でピンの並びの中心から USB へ 5.1855 / 9.3855、幅で ±4.3（2026-09-24 に取り直して読んだ）
XIAO_BOTTOM_SLOTS = {
    "USB_SHIELD_1": (-6.036, -4.758, -4.207, -3.842),
    "USB_SHIELD_2": (-6.036, 3.842, -4.207, 4.758),
    "USB_SHIELD_3": (-10.211, -4.745, -8.432, -3.855),
    "USB_SHIELD_4": (-10.211, 3.855, -8.432, 4.745),
}

# 利用者が挿す USB-C プラグ。金属は規格値、樹脂は HHKB で利用者が測った手持ちの最大
USB_PLUG_SHELL = (8.34, 2.56)
USB_PLUG_BODY_W = 12.0       # [暫定] 樹脂の幅（HHKB で利用者がケーブル数本を測った最大）
USB_PLUG_BODY_H = 7.0        # [暫定] 樹脂の厚さ（同上）
USB_SHELL_EXPOSED = 1.0      # [暫定] 挿しきったときに見える金属（HHKB 実機 ＋ ケーブル 1 本）

# ---------------------------------------------------------------------------
# 右の角: 電池ホルダ BS-16-B4AK003（MYOUNG・LCSC C964732）＋ CR1632
# ---------------------------------------------------------------------------
# 図面 MY-CP-0085（LCSC のデータシート・2020-04-01）: 全長 27.0（端子込み）・本体 23.70・
# 幅 16.00・高さ 5.50・電池の座 φ16.10。推奨ランド 3.75×4.5 を外外 28.0 で 2 つ
HOLDER_PAD = (3.75, 4.5)
HOLDER_PAD_SPAN = 28.0
HOLDER_BODY = (27.0, 16.0)   # 端子込みの長さ × 本体の幅
HOLDER_H = 5.5
CELL_D = 16.1                # 座の直径（電池 φ16.0・Energizer CR1632 データシート）
CELL_T = 3.2
# 中心。x: **電源スイッチ（右の縁）の左に寄せる**（2026-09-24 利用者の決定で電源スイッチを
# ホルダの右へ置いた）。＋のパッドの外端を電源スイッチの本体（最大）から PSW_HOLDER_CLEAR 離す:
# 141.225 − 1.95 − 0.5 − 14.0 = 124.775。そこから 0.075 左の 124.7（2 つのコートヤードを
# 触れさせない）。前は 128.5（＋のパッドの外端を基板の縁から 0.675）で、**左へ 3.8 動かした**。
# 左の柱との間（電池を取り出す指の場所）は 7.4 残る（検査の下限 3.0）。
# y: 段階 1 の位置のまま（ふたの下の空間に 0.2 以上で入る。動かす理由が無い）
HOLDER_AT = (124.7, -38.6)
# ふたを 1 本のネジ（上から）で留める柱。トレイから立ち、基板の穴 LID_PILLAR_HOLE を通る。
# 電池を取り出すときに指・道具が入る側（ホルダの左）に置く
LID_PILLAR_AT = (101.0, -38.6)
LID_PILLAR_D = 5.2           # M2 熱圧入インサート（外径 3.2）＋ 肉 1.0
LID_PILLAR_HOLE = 6.0        # 基板の穴（柱 ＋ 片側 0.4）

# ---------------------------------------------------------------------------
# 電源スイッチ SS-12D00G3（XIAMEN JINBEILI・秋月電子 115707）。**表・スルーホール・レバーが上**。
# 右のふた（電池のふた）の穴からレバーの先が覗き、爪で上から動かす。**利用者が手はんだ**（NOT_ASSEMBLED）
# ---------------------------------------------------------------------------
# 2026-09-24 利用者の決定で MK-12C02-G025（LCSC・裏・横向きのつまみ）から置き換えた
# （決定記録 decisions/2026-09-24-power-switch.md）。
# 図面（秋月の PDF 5 ページ目 DRWG. NO: SS12D00GXX-A03・2020-06-15）と仕様書 JBL-SS/SK-2010:
#   1P2T・ノンショーティング・DC 30V 0.3A・接触抵抗 100mΩ 以下・10,000 回・作動力 230±80gf・
#   本体 8.5 × 3.7・高さ 3.5±0.1・レバー 1.5 角が本体の上に X = 3.0・ストローク 2・
#   足 0.5 × 0.3 が 2.5 間隔で 3 本、台座の下面から 3.9±0.2・推奨の穴 φ0.8 × 3・共通は真ん中。
#   一般公差: 1.5 以下 ±0.1・1.5 を超える ±0.2
# 本体の長辺を y に置く（x に置くとホルダを 8.5＋α 動かすことになり、左の柱の 3.0 を割る）
PSW_BODY = (3.7, 8.5)        # x × y
PSW_BODY_TOL = 0.2           # 一般公差（1.5 を超える）。本体の外形の最大は 3.9 × 8.7
PSW_H = 3.5                  # 本体の高さ（台座の下面から上面まで）
PSW_H_TOL = 0.1              # 図の 3.5±0.1
# [暫定] 枠（金属）の爪が台座の下面から下へ出る量。図面にも写真にも台座の下へ折り返した爪が 4 つ
# 描かれている（側面図は外側の足の脇・端面図は長辺の縁）が**寸法の記載が無い**。図面（縮尺 4:1）を
# 600dpi で読んで 0.39（側面図）・0.30（端面図。監査 audit-psw.md 軽微 1 で 0.37 は読み違いと判明
# ＝ 41px ÷ 135px/mm）、秋月の商品写真で本体の高さ 3.5 を物差しに 0.36。読みの範囲は 0.30〜0.39。
# 0.4 は大きい側（本体が低くなる・レバーの出の検査には安全側）。
# 足で基板に押し付けると爪が基板に当たり、本体はこの分だけ浮く。届いたらノギスで測る
PSW_TAB = 0.4                # [暫定] 上の理由
PSW_LEVER = 1.5              # レバーの断面（1.5 角）
PSW_LEVER_TOL = 0.1          # 一般公差（1.5 以下）
PSW_LEVER_H = 3.0            # 本体の上面からレバーの先まで（図の X = 3.0）
PSW_LEVER_H_TOL = 0.2        # 一般公差（1.5 を超える）
# ストローク。レバーの中心は本体の中心から ±PSW_TRAVEL/2（上面図の窓〔約 3.65〕が本体の中心に
# あることを図から読んだ。窓 = レバー 1.5 ＋ ストローク 2 ＋ 隙）
PSW_TRAVEL = 2.0
PSW_PIN_PITCH = 2.5
PSW_PIN = (0.3, 0.5)         # 足の断面 x × y（端面図 0.3・側面図 0.5。長辺の向きに 0.5）
PSW_PIN_L = 3.9              # 台座の下面から足の先まで
PSW_PIN_TOL = 0.2            # 図の 3.9±0.2
PSW_DRILL = 0.8              # 図の推奨 3-φ0.8（足の対角 0.58 に 0.22）
PSW_PAD_D = 1.6              # ランド。アニュラ 0.4（JLC の下限 0.18・推奨 0.25 の上。こてで付ける）
# 付けたあと、足を基板の下面からこの長さに切る（ニッパー）。切らないと足の先は基板の下面から
# PSW_TAB を引いて 3.9 − 0.4 − 1.6 = 1.9（最悪: 足 4.1・爪 0.36・基板 1.44 で 2.3）出て、下の空き
# UNDER_PCB 1.8 を越え床に当たる。**床は厚くしない・掘らない**（床 1.2 は肉厚の下限。O10）。
# 1.0 ならはんだのフィレットを残して床と 0.8 空く。**キーのスイッチの足は切らない**（1.4 出て床と 0.4）
PSW_PIN_TRIM = 1.0
# 本体（最大）とホルダの＋のパッドの間。2 つのフットプリントのコートヤードの余白（0.25 ずつ・
# KiCad の決まり）が重ならない最小。ホルダを先にはんだ付けするので、こてはこの隙に入らなくてよい
PSW_HOLDER_CLEAR = 0.5
# 本体の中心。x: 本体（最大 3.9）の右の縁を基板の縁 143.175 に揃える: 143.175 − 1.95 = 141.225
# （ホルダを動かす量を最小に。本体が基板の外へ出ると、基板を上から落とすときの壁との隙 0.5 を食う）。
# y: ホルダの中心の高さ（＋のパッドから共通の足へ真っすぐ引ける）。レバーの穴はふたの手前の縁から 8.1、
# 奥の縁から 6.5、右の外面から 2.7
PSW_AT = (141.225, -38.6)
# 入の向き（+1 = レバーを奥 +y へ・−1 = 手前 −y へ）。図面の側面図はレバーを片側に描き、回路図は
# 共通と**同じ側**の端子を繋いで描く（スライドの接点はレバーの下にあり、真ん中とレバーの側の足を
# 渡す）→ レバーの側の足が繋がる。奥の足（パッド 1）を VBAT_SW にして、レバーを奥へ押すと入。
# ふたに刻印する（case.lid_right）。**図の読み。部品が届いたらテスターで確かめる**（assembly-guide 手順 4）
PSW_ON = 1

# ---------------------------------------------------------------------------
# スタビ（Choc V2 用・基板にねじ留め。遊舎工房 A050001-01-1 ×4）の箱の穴（O2）
# ---------------------------------------------------------------------------
# 穴は mech.CHOC_V2_STAB_HOLES["box"] を (x, y) だけ広げて Edge.Cuts で抜く。
# x: 箱（mech.CHOC_V2_STAB_SOURCES の図の part 5.80 × 7.30）との隙が、支点が 24.0 でも 23.8 でも片側 0.4 以上（JLC の外形公差 ±0.2 ＋ 0.2）。
# y: 広げない。広げるとねじの穴との橋が細る（mech.CHOC_V2_STAB_HOLES のコメント）。橋は 0.75 以上
#    （tests/test_cckb_interface.py が見る）。箱との y の隙 0.1 はサリチル酸さんの実物の値で、見本で確かめる
STAB_RELIEF_MARGIN = (0.3, 0.0)
# 基板の下へ出る物（図の側面図。基板の上面から、ねじの頭だけ基板の下面から）
STAB_BOX_L = 3.30            # 箱の下端（基板の上面から下へ。公差は図に無い）
STAB_CLAW_L = 2.65           # ワイヤの側の爪（同）
STAB_SCREW_HEAD_H = 1.15     # ねじの頭（基板の下面から下へ。φ2.78）

# ---------------------------------------------------------------------------
# 取付（O4）。M2×6 皿ネジを**全部下から**。頭は底面から沈める（深さは先端の位置から導く:
# interface.z の screw_head = ナットの上面 ＋ SCREW_PAST_NUT − SCREW_L。床 1.2 でいま 0.5）。
# 相手は
#   - キーの下（9）: 基板の上面に置いた M2 六角ナット（二面幅 4.0・厚さ 1.6）。プレートの六角の穴
#     （二面幅 MOUNT_POCKET_AF）が回り止め
#   - 左の角（H3）: 左のふたのボスの M2 熱圧入インサート。ボスの下面を基板の上面に締める
#     （ナットではふたが留まらない。decisions/2026-09-24-case.md §4-1）
# 先端は 0.5 + 6 = 6.5（ナット 4.6〜6.2 を抜け、プレート上面 6.8 より下。H3 のインサートに 1.9）
# ---------------------------------------------------------------------------
# 位置は projects/cckb/tools/find_mounts.py の候補から選んだ（決定記録 §5-3）。
# 5 条件（縁・キーとプレート開口・部品・コートヤード・配線の通り道）を
# tests/test_cckb_interface.py が生成した基板の実物と突き合わせる
MOUNTS = {"main": [
    (-139.43, 19.57),        # Tab の左（左の奥）。V2 で 0.25 左へ: スイッチの開口 13.8 → 13.95 でナットの穴の肉が 0.01 欠けた
    (-116.18, 20.07),        # Tab の右（左の奥・内）
    (-138.18, -0.43),        # Ctrl の左（左の中）
    (-118.6, -31.8),         # 左の角のふた（XIAO の横。アンテナのチップから 6.6）
    (-67.68, -40.93),        # 左 Meta と左スペースの間（左の手前）
    (-22.68, -40.93),        # 真ん中のスペースの左端（継ぎ目の左）
    (48.32, -40.93),         # 右スペースの右端（継ぎ目の右）
    (93.82, -21.43),         # 右 Shift の左（右の中）
    (116.82, 20.07),         # Del の左（右の奥）
    (139.35, 14.95),         # Del の右下（右の端）。**V2 で動かした**: 前の Enter の右 (137.82, −6.93) は V2 の
                             # スタビ（右の箱の穴・ねじの穴・プレートの羽）に当たり、Enter の右には置ける所が無い
                             # （find_mounts: 羽の縁 136.63 ＋ 3.62 がプレートの縁 − 3.62 を越える）。右の端の候補は
                             # x 139.2〜139.5・y 13.3〜16.6 だけで、その真ん中（x の余裕 ±0.15）。代わりの案は
                             # 右 Shift と Fn の間 (120.5, −17) 付近（候補が広い）。前の場所には支えを立てた
]}
# 基板の面に当たる金属（キーの下 9 個の六角ナット・H3 のインサート。interface.metal_on_pcb が
# 取付ごとに高さから判定する）の当たる円から、当たる側の銅（配線・ビア・ベタ）を離す量。
# pcb_extra が禁止域 METAL_KEEPOUT を置く。締め付けでマスクが欠けると信号が GND や金属に落ちる
# （1 回目の監査 E 重要 3: H3 の下の CS。2 回目の監査 E 重要 1: 配線し直しで H5・H6 のナットの下に CS）
METAL_COPPER_CLEAR = 0.3
MOUNT_POCKET_AF = 4.2        # プレートの六角の穴の二面幅（ナット 4.0 ＋ 0.2）
MOUNT_POCKET_WEB = 1.2       # 穴とほかの開口の間に残す肉（0.4×3）
MOUNT_BOSS_D = 5.6           # トレイのボス（基板の下）の直径
NUT_AF = 4.0                 # M2 六角ナット（ISO 4032）の二面幅
NUT_T = 1.6                  # 同 厚さ
SCREW_L = 6                  # M2×6 皿ネジ（ISO 10642 / DIN 965。長さは頭を含む）
SCREW_HEAD_H = 1.2           # 皿の頭の高さ（M2）
# ネジの先をナットの上面から出す量。**床の高さが動いても頭の沈め（interface.z の screw_head）が
# 追いかける**ように、沈めではなくこれを持つ（前は沈めの深さ 0.9 を直に書いていて、床 1.2 だと
# 先 6.9 がプレートの上面 6.8 を越えた）。ナット 1.6 = 4 山を全部かけ、先の面取りの分 0.3 出す。
# 下からの皿の頭は底面から 0.5 沈む（机に当たらない。検査は 0.5 以上）
SCREW_PAST_NUT = 0.3
# 基板を下から支える柱（ネジ無し・トレイから立つ）。打鍵でたわむ長さを縮める（決定記録 §2-4）
# 段の境目の帯（配線の無い所）に 2u おき。interface.support_problems の候補から、
# 取付のボスと 12mm 以上離して選んだ。
# **V2 で 8 本を動かした**（どれも最寄りの候補。support_problems が板の実物で見る）:
#   段 3|4 の帯の 5 本（y −26.43 → −27.03）: V2 の位置決めの長穴（キー中心の 5.15 下・1.6 × 2.0）に当たった
#   (−133.18, −26.43) → (−130.88, −28.53): 左 Shift の左のスタビのねじの穴
#   (−57.18, −26.43) → (−54.83, −25.73): 長穴と左スペースのスタビの爪の穴の両方
#   (18.82, −44.43) → (18.57, −44.43): 右スペースの長穴
#   (133.32, 11.07) を外し、(137.72, −9.83) に立てた: 前者は Enter の右の爪の穴に当たり、H9 の新しい位置の
#   7mm 内。後者は H9 の前の場所（Enter の右）の支えの代わり
#   **2026-09-26 さらに 6 本を 0.5〜0.55 動かした**: 床に足の止まり穴（位置決めの長穴 ＋ 0.4）を掘ったので、柱の根元と
#   穴の間に肉 POCKET_WALL 0.4 が要る（interface.support_problems）。段 3|4 の 5 本は y −27.03 → −27.53（左スペースの左は
#   x −54.83 → −54.28）、(18.57, −44.43) → (18.07, −44.43)。どれも最寄りの候補（0.05 おきの総当たり）
SUPPORTS = [
    (-130.68, 46.07), (-98.18, 44.57), (-54.18, 44.57), (-16.18, 45.57),
    (16.32, 46.07), (59.82, 46.07), (92.32, 44.57), (136.32, 44.57),
    (-95.18, 28.57), (-57.18, 28.57), (-19.18, 28.57), (18.82, 28.57),
    (57.32, 28.57), (95.32, 28.57), (133.32, 28.57), (-95.18, 9.57),
    (-57.18, 9.57), (-19.18, 9.57), (18.82, 9.57), (57.32, 9.57),
    (95.32, 9.57), (137.72, -9.83), (-95.18, -9.43), (-57.18, -9.43),
    (-19.18, -9.43), (18.82, -9.43), (57.32, -9.43), (95.82, -8.93),
    (-130.88, -28.53), (-95.18, -27.53), (-54.28, -25.73), (-19.18, -27.53),
    (18.82, -27.53), (57.32, -27.53), (133.32, -26.43), (-95.18, -44.43),
    (18.07, -44.43), (94.82, -44.43),
]
SUPPORT_D = 3.0

# ---------------------------------------------------------------------------
# A1 mini（D12・D13）: 印刷する部品はすべて平面で 168.4 角以内
# ---------------------------------------------------------------------------
PRINT_MAX = 168.4
# プレートの分割: 段ごとに、分ける x（キーの境目）。上の段から。段と段の間は
# 段の境目（y）に沿って横に走る。どれもスイッチ・スタビの開口を切らない
PLATE_SPLIT = {"main": (9.525, 0.0, 4.7625, -4.7625, 4.7625)}
# トレイの継ぎ目（案 C・利用者に確認中）: 奥の壁は最上段の 7 と 8 の間、手前の壁は
# 真ん中と右のスペースの間の、**キャップの隙間の線の上**に置く。床は y = 0 で段を付ける
CASE_SEAM = (9.525, 4.7625)  # (奥の壁の x, 手前の壁の x)

# プレートの開口（キー以外）。最下段の空いた角。XIAO（左）と電池ホルダ（右）が基板の
# 表に立つので、プレートを抜く（プレート下面と基板の隙間は 1.0mm しかない）。
# (中心 x, 中心 y, 幅, 奥行)。内側の 2 辺は角のセルの境目ちょうど、外側の 2 辺は
# プレートの外形の外まで（外周に細い帯を残さない）。tests/test_cckb.py が配列から導いた
# 角と照合する
PLATE_OPENINGS = {"main": [
    (-142.875 + 0.75 * 19.05 - 1.0, -38.1 - 1.0, 1.5 * 19.05 + 2.0, 19.05 + 2.0),
    (-142.875 + 13.75 * 19.05 + 1.0, -38.1 - 1.0, 2.5 * 19.05 + 2.0, 19.05 + 2.0),
]}

MATRIX_COLUMNS = "physical"

# スタビのキー 4 つのダイオード（O7）は V2 では**上書きしない**: mech.CHOC_V2.diode_offset（7.1, −1.0）が
# 箱の穴からコートヤードまで 0.35 を残す（tests/test_cckb_interface.py::test_the_stab_relief_clears_the_diodes）

# ---------------------------------------------------------------------------
# 行列の配線（設計書 §12・projects/cckb/matrix_routes.py が引く）。CAD の向き
# ---------------------------------------------------------------------------
# 行のバス（裏・横一直線）の高さ。キー中心から下へ。V2 は中心穴 φ5.05（下端 −2.525）と位置決めの長穴
# （x −5・y −5.15 の 1.6 × 2.0。上端 −4.15）の**間**しか横に通れない（V1 のボス φ1.9 は無くなった）。
# 穴と線の縁の間は 0.3（JLC の穴と銅）＋ 線の半幅 0.1: −2.925 〜 −3.75。真ん中の −3.35（両側 0.425）。
# スタビの箱の穴・ねじの穴・取付の穴に当たる所は matrix_routes が下へ回り道させる（plan の detour）
ROW_BUS_DY = (-3.35, -3.35, -3.35, -3.35, -3.35)
# ---------------------------------------------------------------------------
# キー以外の部品（projects/cckb/pcb_extra.py が置く。回路は circuit.py）
# ---------------------------------------------------------------------------
# XIAO のフットプリント（lib/xiao.pretty/XIAO_nRF52840_SMD）の原点はピンの並びの中心。
# 本体の中心（XIAO_AT）はそこから USB の側へ 0.064 ずれている（公式 STEP の
# キャステレーション 14 個の中心 1.7405 と基板の中心 1.8045。tests/test_cckb_pcb.py が測り直す）
XIAO_PIN_SHIFT = 0.064
# 裏の電子部品（JLC が実装・D8）。(x, y, 角度)。CAD の座標、角度は KiCad で**裏返す前**に回す量。
# 置き場所の理由:
#   U1・U2  段 2|3 の帯（y −8.2）。行 2 のバス（−4.3）とパッドの間 1.3、下の段の端子 1
#           （上端 −12.05）とコートヤードの間 1.1。支えの柱（y −9.43 の x ±95.18・±57.18）の間。
#           列 0..7 / 8..14 の真ん中（列 3.5 = −66.7 / 列 11 = 76.2）の近く
#   C_U*    各 595 の VCC（16 番。裏返すと左上）の左 3.1（パッドの端どうし 0.95）
#   D_PWR・R_HI・R_LO  XIAO の奥の縁のすぐ上、段 3|4 の帯の左端（裏）。**XIAO の真下には
#           置かない**: XIAO の裏の露出パッドのため XIAO の下は表の銅（ビアを含む）を禁止して
#           いて、そこに置いた R_LO の GND にビアを打てる所が 5mm 以内に無かった（2026-09-24）。
#           支えの柱（−133.18, −26.43・φ3.0）と行 3 のバス（y −23.35）、XIAO から上がる行 0〜3
#           の縦線（x −135.15〜−127.53・XIAO_ESCAPE）を避ける。分圧は D0（手前の列）から XIAO の下を
#           約 20mm（VBAT_SENSE は 500kΩ の高インピーダンス。ADC は取り込みの間だけ読む）
#           **D_PWR は V2 で (−123.0, −25.8) → (−124.0, −28.45)**: V2 では行 3 のバス・XIAO からの行 3 の線・
#           左 Shift の右のスタビのねじの穴が D_PWR を囲み、VBAT_SW が右（電源スイッチ）へ出られなかった
#           （Freerouting が 4 つの余裕とも VBAT_SW を 1 本引けない・2026-09-25）。行 3 の回り道（y −27.4）の
#           下・XIAO の奥の縁（−29.21）の上の帯に下ろし、裏の VBAT_SW が帯（y −28.4 は x −126〜−59.7 が空く）を
#           右へ行けるようにした。H3 のボス（φ5.6）からコートヤードまで 0.96・行 3 の縦線（x −127.53）から
#           パッドまで 1.33
PART_AT = {
    "U1": (-76.2, -8.2, 0), "U2": (76.2, -8.2, 0),
    "C_U1": (-82.2, -5.925, 0), "C_U2": (70.2, -5.925, 0),
    "D_PWR": (-124.0, -28.45, 0),
    "R_HI": (-140.9, -27.7, 0), "R_LO": (-140.9, -24.9, 0),
}
# 名札（リファレンス）の置き場所。部品の中心から (dx, dy)・CAD。**消さない**（HHKB の作法）。
# 既定の位置は隣の部品のパッド（R）・自分の外形の線（595）に重なった（DRC の silk 警告）
REF_TEXT_AT = {"R_HI": (3.2, 0.0), "R_LO": (3.2, 0.0), "U1": (0.0, 4.6), "U2": (0.0, 4.6)}
# 電源の太さ（pcb_extra がネットクラス POWER にする）。流れるのは mA 級（BLE の最大 10.22mA・
# HHKB 実測）なので電流では決まらない。**長さ 250mm の VBAT_SW の電圧降下**: 1oz（35µm）
# 0.3 幅で ρ/(w·t) = 1.72e-8/(0.3e-3·35e-6) = 1.64Ω/m → 0.41Ω・10mA で 4.1mV（0.2 幅なら 6.1mV）。
# 電池の内部抵抗 約 25Ω（設計書 D4）に比べて 2% 未満。太いほど傷に強い
POWER_TRACK_W = 0.3
# 電源の長い線を**決まった形で**引く（matrix_routes.power_runs・裏・POWER_TRACK_W）。Freerouting は
# V2 の板で VBAT_SW の長い 1 本（D_PWR → 電源スイッチ・約 280mm）を 4 つの余裕とも引けなかった（2026-09-25）。
# V1 の通り道（段 3|4 の帯 y −28.39）は、V2 のスペースのスタビの爪の穴（y −31.93〜−27.53）が 4 か所で塞ぐ。
# 道: D_PWR のアノードから下りて XIAO の奥の縁の上（y −29.5）を右へ、左 Shift の右のねじの穴と行 3 の回り道
# （x −105.64 まで）を過ぎた x −103.5 で段 3 の長穴の下（y −26.4。長穴の下端 −25.2 とスペースの爪の上端 −27.53 の間）
# へ上がり、右の端（x 139.4）まで行って電源スイッチの足へ下りる。**y −29.5 の区間は短くする**（前は x −61 まで
# 引いていて、段 3|4 の帯の裏を塞ぎ、COL1〜3 などが Freerouting で 1 本引けなかった）。
# 残る R_HI までの短い 1 本は Freerouting が引く（route_pcb が DSN のピンを絞る）
POWER_RUNS = {"VBAT_SW": dict(frm=("D_PWR", "2"), to=("SW_PWR", "1"), layer="B.Cu",
                              pts=[(-125.65, -28.45), (-125.65, -29.5), (-103.5, -29.5), (-103.5, -26.4),
                                   (139.4, -26.4), (139.4, -36.1), (141.225, -36.1)])}
# GND のベタへの繋ぎ方をサーマル（スポーク）にするパッド。ほかの GND パッドはベタへ直結（FULL）。
#   C_U1.2・C_U2.2・R_LO.2  JLC がリフローで付ける 0805。片側だけがベタに直結だと熱の差で
#           立ち上がる（ツームストーン）。JLC の組立規約 Notes on DFM 4「熱の設計が無いと
#           tomb-stone が起きうる」（監査 C 軽微 2）
#   BT1.2・U_MCU.GND  利用者がこてで付ける。直結だと両面のベタと 900 個超のビアに熱が逃げて
#           温まらない（監査 C 軽微 1）
# 595 の 8・13 番（TSSOP）は 2 端子ではなく立ち上がらないので直結のまま（GND の戻りを細くしない）。
# どのパッドも GND のスタブ → ビア（route_pcb の gnd_fanout）で別に繋がっている
THERMAL_PADS = {"C_U1": ("2",), "C_U2": ("2",), "R_LO": ("2",), "BT1": ("2",), "U_MCU": ("GND",)}

# XIAO から行のバスまで（行 0〜4）。**決まった形で引く**（matrix_routes.escape）。Freerouting は
# 5 本とも XIAO から出せなかった（2026-09-24: XIAO の下は裏しか使えず、左の角は逃げ穴・穴・
# SPI の束で塞がり、D0〜D6 の真ん中に VBAT_SENSE の D0 がいる）。
# 道順（CAD・パッド内ビア（手前の縁）から）:
#   行 0〜2  裏を turn_y まで上がり（XIAO の下。裏は使える）、裏を左へ → 裏を上へ XIAO の奥の外のビア（via）→
#            表を左へ → 左の縁の通り道（lane_x）を表で上へ → そのバスの高さでビア → 裏でバスの左端へ。
#            **V2 で変えた**: 左 Shift の左のスタビ（ねじの穴 x −134.94〜−131.74・y −26.95〜−23.55 と箱の穴
#            x −136.74〜−130.04・y −23.35〜−14.75）が、V1 の「真上へ → ビア → 表を左へ」の道を塞ぐ。
#            そこで XIAO の下で先に左へ寄せ、スタビより左（x −138.2〜−137.0）でビアへ上がる。
#            turn_y は 0.6 ずつ（行 0 が一番下）: 左へ行く裏の線が、右の行の縦の線をまたがない。
#            ビアは 0.8 ずつ上下・0.6 ずつ左右にずらし、表を左へ行く線が隣のビアを避ける。
#            **表の通り道は D0 の VBAT_SENSE（裏）と交わらない**
#   行 3     裏を run_y まで上がり（左 Shift の位置決めの長穴 y −25.2 と D_PWR の下）、右へバスの左端の x まで → 上へバス
#   行 4     裏を run_y まで上がり、アンテナの禁止域の下（y −44.1 より下）を右へ Alt のダイオードの
#            x まで → 上へバス
# 左の縁の通り道 −142.45〜−141.25（0.6 おき: 通り道の端のビア φ0.6 が隣の通り道から 0.2）:
# 縁の配線禁止の帯（−142.855）から線の縁まで 0.305、H0（φ2.2・縁 −140.28）から 0.87
XIAO_ESCAPE = {
    "ROW0": dict(turn_y=-32.0, via=(-138.2, -26.0), lane_x=-142.45),
    "ROW1": dict(turn_y=-31.4, via=(-137.6, -25.2), lane_x=-141.85),
    "ROW2": dict(turn_y=-30.8, via=(-137.0, -24.4), lane_x=-141.25),
    "ROW3": dict(run_y=-27.4),        # 左 Shift の右の箱の下の回り道（matrix_routes.buses）と同じ高さ
    "ROW4": dict(run_y=-46.3),
}

# 列（表）を端子 1 からキー中心の左へ寄せる量。線の中心から、中心穴 φ5.05 の縁（2.525）まで 0.575、
# 位置決めの長穴（x −5 ± 0.8。縁 −4.2）まで 1.1（V1 はボス φ1.9 の縁から 1.35 で決めていた。V2 でボスは無い）
COL_JOG = 3.1

ZMK = {
    "shield": "cckb",
    "board": "xiao_ble//zmk",
}

# ---------------------------------------------------------------------------
# 発注（JLCPCB の実装）。foundry.fab_fields が板に LCSC・面を焼く。**裏の片面だけ**（D8）
# ---------------------------------------------------------------------------
# 参照名 → 種類（fullmatch。キーの SW\d+・D\d+ は核が持つ）
FAB_KINDS = {r"U[12]": "74LVC595", r"C_U[12]": "cap_100n", r"R_(HI|LO)": "res_1M",
             "D_PWR": "schottky", "SW_PWR": "power_switch", "BT1": "coin_holder",
             "U_MCU": "xiao_nrf52840", "H_LID": "hole"}
# JLC に実装させない物（BOM・CPL から外す）: 利用者が手はんだする XIAO・ホルダ（表・D8）と
# スイッチ（直付け・D3。LCSC に無い）、穴
# **電源スイッチも手はんだ**: SS-12D00G3 は秋月電子で買うスルーホールの部品で LCSC に無い
# （2026-09-24 に置き換え。前の MK-12C02-G025 も監査の裁定 R2 で手はんだにしていた）。
# 手はんだの物は、LCSC の番号が無ければ PARTS に載せない（買う所は docs/shopping-list.md）。ホルダ（coin_holder）は
# 利用者が LCSC でも買えるので番号だけ持つ（NOT_ASSEMBLED で BOM・CPL からは外れる）
NOT_ASSEMBLED = {"xiao_nrf52840", "coin_holder", "keyswitch", "hole", "power_switch"}
# 種類 → LCSC。選んだ理由（効く条件を全部並べて比べた記録）は docs/parts-audit.md。
# 在庫・単価は `tools/kb cckb parts`（JLCPCB の API）で発注の直前に取り直す
PARTS = {
    # BAT46W: VF ≦0.25V@0.1mA と IR ≦0.3µA@1.5V を動作点で両方保証（foundry/parts.py の比較）。
    # CR1632 のレールは打ち止めでも 2.0V 級（D_PWR の後ろ）で、行の VIH 0.7×レールに対して
    # 列の High − VF が足りる（レール ≧ (0.25 + 0.2)/0.3 = 1.5V）
    "diode": {"lcsc": "C54110", "desc": "BAT46W SOD-123 ショットキー 100V"},
    # 74LVC595: 1.1〜3.6V（74HC595 は下限 2.0V で打ち止めに規格外）。TSSOP-16
    "74LVC595": {"lcsc": "C52287685", "desc": "SN74LVC595APWR TSSOP-16 (1.1〜3.6V)"},
    # D_PWR（電池 → 3V3）。**BAT46W（キーと同じ品）**: USB 給電中に電源スイッチ ON でも、25℃ では
    # 逆漏れの保証値 0.3µA@1.5V が分圧の吸う 0.93µA（1.9V）より小さく、電池へ入らない。Vf の保証値も
    # ≦0.45V@10mA（B5819W は 1A の 0.6V しか保証が無い）。板は同じ D_SOD-123 で変わらない。
    # **2026-09-25 に決まった**（open-gaps O12。保証値で決まる。起動の余裕は task-10a §6 がこの Vf の保証値で見る）。
    # 比べた表: decisions/2026-09-24-audit-fixes.md §S2（2 回目の監査 A2-1）
    "schottky": {"lcsc": "C54110", "desc": "BAT46W SOD-123 ショットキー 100V（D_PWR・キーと同じ品）"},
    "cap_100n": {"lcsc": "C49678", "desc": "0.1uF 0805 50V X7R（Basic）"},
    "res_1M": {"lcsc": "C17514", "desc": "1MOhm 0805 1%（Basic）"},
    # 利用者が手はんだ（NOT_ASSEMBLED）。国内 eleshop でも同じ品番。決定記録 §2-1
    "coin_holder": {"lcsc": "C964732", "desc": "MYOUNG BS-16-B4AK003 CR1632 ホルダ（手はんだ）"},
}
